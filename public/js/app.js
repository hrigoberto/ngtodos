angular.module('ngtodos', []);
// creates a new angular application (this is called the setter syntax)
// word within the quotes is the name of the module, the array is there to input angular scripts others have created
